# Общие решения

Реестр решений, действующих для обоих участников. **Решение действует только тогда,
когда его подтвердили оба** — Григорий ([@tonclap](https://github.com/tonclap)) и
Алексей ([@meta1ex](https://github.com/meta1ex)). Одна подпись — это предложение,
а не решение.

Зачем отдельный файл: в парной работе через агентов легко принять за общее то, что
проговорил только один. Здесь лежит то, на что можно ссылаться как на «мы
договорились», — и ничего кроме.

## Как подтверждать

1. Вносишь решение в таблицу «На подтверждении» — формулировкой, а не намёком:
   так, чтобы через месяц было понятно без разговора.
2. Ставишь себе `✅ дд.мм` в свою колонку, второму оставляешь `—`.
3. Второй участник читает и **сам** ставит свою отметку отдельным коммитом от
   своего аккаунта — за другого не расписываются, в том числе агент за человека.
4. Как только отметок две — строка переезжает в «Действующие решения».

Не согласен — не молчи и не стирай строку: пиши возражение в колонку «Заметки»
или заводи альтернативную формулировку рядом. Разногласие в реестре полезнее, чем
его отсутствие.

## Действующие решения

Подтверждено обоими, можно ссылаться.

| # | Решение | Григорий | Алексей | Заметки |
| --- | --- | --- | --- | --- |
| D-001 | Ведём этот реестр: «мы договорились» = строка с двумя отметками здесь | ✅ 01.09 | ✅ 01.09 | Заведён 01.09.2026 |
| D-002 | Канал работы — общий репозиторий с прямым доступом, без форка и PR | ✅ 01.09 | ✅ 01.09 | Предложено Алексеем в [AGENTS.md](AGENTS.md) |
| D-004 | Начинаем оба со стадии **explore**: до любой сборки — разведка задачи | ✅ 01.09 | ✅ 01.09 | Предшествует стадиям из [INTENTION.md](INTENTION.md) (хаос → сборка → MVP) |
| D-005 | Explore **не делится** между нами: оба проходим её целиком и независимо, затем сводимся по расхождениям | ✅ 01.09 | ✅ 01.09 | Дублирование здесь намеренное: расхождения двух разведок — и есть результат |

D-005 сознательно противоречит привычному «раздели работу, чтобы не делать дважды»:
на explore параллельная работа обоих — не потеря, а инструмент. Совпало у двоих —
похоже на факт; разошлось — там и лежит настоящая сложность задачи.

## На подтверждении

| # | Решение | Григорий | Алексей | Заметки |
| --- | --- | --- | --- | --- |
| D-003 | Граница прямого push в `master` проводится по **типу объекта**, а не по расположению файла. Прямым push — документы процесса и разведки (`INTENTION.md`, `ROLES.md`, `COMMON_DECISION.md`, `AGENTS.md`, `README.md`, `CHANGELOG.md`, `TASK_DESC.md`, `about/**`, `research/**`); через pull request — код и конфигурация прогона (`*.py`, `config.yaml`, `requirements.txt`, `.env.example`, `repo.yaml`) | ✅ 01.09 | — | Переформулировано 01.09 по устной договорённости; прежняя формулировка — в «Отменённых». Отметка Алексея снята: она стояла под другим текстом |
| D-007 | Force-push в `master` и удаление чужих веток запрещены безусловно — обоим участникам, включая владельца репозитория | ✅ 01.09 | — | Независимо от D-003: тот регулирует обычный push и этого не покрывает |
| D-008 | Транспорт решений — pull request. Изменения `COMMON_DECISION.md` вносятся только через PR из ветки `decision/D-00X-краткое-имя`; **approve второго участника = его подпись**, merge = переезд решения в «Действующие». Исключение к D-003: единственный документ, для которого прямой push в `master` закрыт | ✅ 01.09 | — | Заведён первым PR по собственным правилам — механика проверяется до того, как станет обязательной |
| D-009 | Форма продукта — **сравнение сценариев**, а не список идей и не только апсайклинг. Выдача ранжирует применимые сценарии из пяти (продать / починить / переделать / отдать / утилизировать) по паре «время человека / остаточная ценность» и **явно отбрасывает неприменимые**. Формулировка `TASK_DESC.md` про «5–7 идей» заменяется | — | — | Единственное место, где разведки тянут в разные стороны: Алексей сужает до идей реюза, Григорий расширяет до decision support. Ниже — довод каждой стороны, подписывать только после спора |
| D-010 | Прогон explore ставится на **три плеча** на одном наборе фото: `claude-opus-5`, второй облачный провайдер, локальная Gemma 4 12B через Ollama. Выбор боевого провайдера — после замера, не до него | — | — | Предложено Алексеем. Локальное плечо решает не цену, а приватность: набор — квартира, детские вещи, интерьер |
| D-011 | Критерий «прототип ✓» переписывается. Годен, если: (1) на ≥10 фото из 15 идеи ссылаются на видимую деталь, а не на класс вещи; (2) на фото, где решающее не видно, выдача **задаёт уточняющий вопрос или помечает неуверенность**, а не отвечает уверенно; (3) на `item_10` и `item_13` неприменимые сценарии отброшены явно. Критерий записывается **до** прогона | — | — | Прежний критерий («сам оцениваю как реально полезные») — оценка задним числом: под неё подгоняется любой результат |

Почему D-009, D-010, D-011. Все три выведены из сверки двух разведок, законченных
01.09.2026 (`research/EXPLORE_tonclap.md`, `research/BUSINESS_meta1ex.md`), и подписей
пока не имеют **ни у кого** — это предложения на спор, а не готовые договорённости.

**D-009 — единственное прямое противоречие между разведками, остальное дополняет друг
друга.** Довод Алексея за сужение: в узкой формулировке (генераторы идей реюза) прямой
конкурент найден точнее, чем в широкой, — Upcycle AI повторяет «Как проверяю» из
`TASK_DESC.md` почти дословно. Довод Григория за расширение: на живых фото `item_10`
(остаток плитки — стройматериал) и `item_13` (одноразовые грелки — расходник) половина
сценариев неприменима в принципе, то есть выдача обязана уметь **отбрасывать ветки**, а
плоский список из 5–7 идей этого не умеет. Компромисс в формулировке выше: сравнение
сценариев с явным отбрасыванием — но проигравшая сторона должна сказать это вслух, а не
молча подписать.

**D-010 закрывает пробел, которого не было видно с технической стороны.** Григорий взял
`claude-opus-5` без альтернатив; Алексей показал, что связка image + schema у Claude не
имеет ни одного примера в официальной документации (нужен smoke-тест), а локальная модель
даёт то, чего не даёт ни один облачный провайдер: фото не покидает устройство. На нашем
наборе это не абстракция.

**D-011 опирается на замер, а не на мнение.** Разведка на 15 фото дала числа: грубое
состояние читается на 11 из 15, решающее для совета — только на 3, уточняющий вопрос
нужен на 12. Поэтому пункт (2) критерия — не пожелание вежливости, а проверка на главную
сложность: **честно ли модель ведёт себя там, где данных на фото нет**.

Почему D-008. Сегодня подпись приходилось передавать устно, и это уже разъехалось:
Алексей подтвердил прежнюю редакцию D-003, пока шло обсуждение другой формулировки.
Approve чинит четыре вещи разом — он привязан к аккаунту, дате и конкретному тексту
диффа; приходит уведомлением сам; отличает «request changes» от молчания; **снимается
автоматически при новом коммите в ветку**, тогда как сегодня отметку Алексея под
переписанным текстом пришлось снимать руками. Побочно закрывается и запрет из D-001
«агент за человека не расписывается»: у агента нет аккаунта, approve он поставить
не может физически.

Галочки в колонках «Григорий» и «Алексей» остаются — **как витрина, а не как
источник правды**: проставляются при мерже, читаются глазами. Источник правды —
approve в PR. Расхождение витрины с approve — дефект витрины.

Чего у D-008 нет. Ruleset на `master` с required review не заводится (отклонено
01.09), поэтому PR-транспорт держится на дисциплине, а не на механизме: технически
прямой push в `COMMON_DECISION.md` по-прежнему проходит, и merge собственного PR
без approve — тоже. Записано явно, чтобы через месяц не казалось, что здесь стоял
гейт. Правило свежести (`git fetch` перед записью, хуком) тоже отклонено — сверку
перед записью делает агент по инструкции, и это пожелание, а не механизм.

Почему D-003 переформулирован. Замысел прежней версии верный — документы движутся
быстро, код медленно, — но граница была проведена географически, и на текущем дереве
давала обратный результат: в корне лежат `main.py`, `config.yaml`, `requirements.txt`,
`repo.yaml`, то есть код шёл бы без ревью, а `about/KNOWN_ISSUES.md` и `research/`
(документы) — через PR. Отдельно: коммит `1e9ff19` — разведка Алексея в `research/`
прямым push в подпапку — по прежней формулировке уже был бы нарушением, а разведки
по D-005 по природе ложатся в подпапку.

К вопросу о механизме: ветка `master` сейчас не защищена, rulesets пусты. Репозиторий
публичный, поэтому ruleset доступен и на плане Free — гейт можно сделать механическим,
а не дисциплинарным. Оговорка: владелец репозитория обходит ruleset молча, пока это
не закрыто явно. Номер D-006 пропущен — его занимала отозванная строка
(коммиты `0ad39f7` → `909d695`), переиспользовать номер значит путать историю.


## Отменённые и пересмотренные

Решение перестало действовать — не удаляем, а переносим сюда с датой и причиной.
История передумываний — часть эксперимента.

| # | Решение | Что стало | Когда и почему |
| --- | --- | --- | --- |
| D-003 (ред. 1) | Прямой push в `master` без ревью — допустим только в корне, в подпапках через pull request | Переформулировано, см. D-003 в «На подтверждении» | 01.09.2026, устно. Граница по расположению файла на текущем дереве давала обратный намерению результат (код в корне — без ревью, документы в подпапках — через PR). Формулировка была подтверждена Алексеем ✅ 01.09 до пересмотра |
