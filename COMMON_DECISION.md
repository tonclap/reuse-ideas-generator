# Общие решения

Реестр того, на что можно ссылаться как на «мы договорились», — и ничего кроме.
Участники: Григорий ([@tonclap](https://github.com/tonclap)),
Алексей ([@meta1ex](https://github.com/meta1ex)).

Нерешённое живёт в [`OPEN_ISSUES.md`](OPEN_ISSUES.md), а не здесь: под расхождением
подписывать нечего.

## Как принимается решение

Правки этого файла идут **только через pull request** из ветки `decision/D-0XX-имя`
(D-008). **Approve второго участника — это его подпись**; merge означает, что решение
действует. Отдельные галочки в таблице не ведём: они дублировали approve и разъезжались
с ним — источник правды один, история PR.

Быстрые развилки можно выносить ask-issue с голосованием реакциями (так закрыт D-018).

Механического гейта нет (D-023): технически прямой push сюда проходит, и merge своего PR
без approve — тоже. Держится на дисциплине, а не на запрете.

## Действующие решения

| # | Решение | Заметки |
| --- | --- | --- |
| D-001 | Ведём этот реестр: «мы договорились» = строка здесь | Заведён 01.09.2026 |
| D-003 | Граница прямого push в `master` проводится по **типу объекта**, а не по расположению файла. Прямым push — документы процесса и разведки (`INTENTION.md`, `ROLES.md`, `OPEN_ISSUES.md`, `AGENTS.md`, `README.md`, `CHANGELOG.md`, `TASK_DESC.md`, `about/**`, `research/**`); через pull request — код и конфигурация прогона (`*.py`, `config.yaml`, `requirements.txt`, `.env.example`, `repo.yaml`), а также сам `COMMON_DECISION.md` | Ред. 3 от 01.09: из списка прямого push убран `COMMON_DECISION.md` — он противоречил D-008. Прежние редакции — в «Отменённых» |
| D-004 | Начинаем оба со стадии **explore**: до любой сборки — разведка задачи | Предшествует стадиям из [INTENTION.md](INTENTION.md) (хаос → сборка → MVP). Закрыта решением D-020 |
| D-005 | Explore **не делится** между нами: оба проходим её целиком и независимо, затем сводимся по расхождениям | Дублирование намеренное и противоречит привычному «раздели работу»: совпало у двоих — похоже на факт, разошлось — там и лежит сложность |
| D-007 | Force-push в `master` и удаление чужих веток запрещены безусловно — обоим, включая владельца репозитория | Независимо от D-003: тот регулирует обычный push |
| D-008 | Транспорт решений — pull request из ветки `decision/D-0XX-имя`; approve второго участника = его подпись, merge = решение действует | Ритуал: Григорий заводит PR → Алексей апрувит → Григорий мержит. Approve привязан к аккаунту, дате и тексту диффа и снимается сам при новом коммите — устная передача подписи уже один раз разъехалась |
| D-009 | Продукт сужается до **reuse-only**: выдача предлагает варианты повторного использования и переделки конкретной вещи. «Продать» и «утилизировать» из ядра исключаются. Если идей реюза нет — сказать прямо, а не добирать до количества | Позиция Алексея, Григорий снял своё предложение. Сужение убирает сложность B2 (цена и рынок) и часть риска фрагментации площадок, но переносит вес на B1/B4: для переделки состояние и материал нужны точнее, чем для продажи |
| D-010 | Транспорт запросов — **OpenRouter**. Модель выбирается по соотношению **цена/качество** на одном наборе фото. Перед выбором проверить, что **конкретный эндпоинт** держит structured outputs (поле `structured_outputs` в карточке маршрута) | Цена решения — приватность: агрегатор добавляет посредника поверх провайдера, локальное плечо это снимало. Проверять надо не «держит ли модель», а «держит ли этот маршрут» |
| D-011 | Критерий «прототип ✓»: (1) на ≥10 фото из 15 идеи ссылаются на видимую деталь, а не на класс вещи; (2) где решающее не видно — уточняющий вопрос или пометка неуверенности, а не уверенный ответ; (3) на `item_10` и `item_13` неприменимые сценарии отброшены явно. Записывается **до** прогона | Опирается на замер: грубое состояние читается на 11 фото из 15, решающее — на 3, вопрос нужен на 12. Пороги придётся пересмотреть из-за D-026 |
| D-012 | Адресат — **любой человек в интернете**. Проект тестовый, пользователь реальный: послабления «это личный инструмент» не действуют, качество фото, галлюцинации и приватность считаются рисками в полную силу | Отменяет скидку на три риска из `research/BUSINESS_meta1ex.md` |
| D-013 | Уточняющий вопрос задаётся только тогда, когда решающая для совета деталь не видна на фото — не всегда и не никогда | Алексей сошёлся с позицией Григория |
| D-014 | На каждое фото — **два независимых прогона**; расхождение между ними служит метрикой неуверенности (пункт 2 критерия D-011) | Алексей сошёлся с позицией Григория |
| D-015 | Выдачу судит **человек глазами** — на explore и в начале build. Полноценная система evaluation — отдельная задача, форма не выбрана | Интерим-ответ Алексея достраивает то, что Григорий оставил отложенным |
| D-016 | Язык выдачи — **русский** | У Алексея с оговоркой «непринципиально» |
| D-017 | Фотонабор **общий для обоих**, а не свой у каждого | Состав задан отдельно в D-026 |
| D-018 | Фотонабор **коммитится в репозиторий**: 15 снимков из `input/` становятся частью проекта, чтобы прогон можно было воспроизвести извне. Публикация необратима — файлы остаются в истории git и после удаления | Голосование в ask-issue #6, 👍 от обоих на варианте A. Алексей при этом **сменил позицию**: в `OPEN_ISSUES.md` у него было «в репозитории не нужны». Цена принята сознательно — в кадре квартира, интерьер, детские вещи, личная электроника |
| D-019 | Таксономия категорий на explore **не чинится**: шесть категорий с переполненным `other` — известное ограничение до build | Обе разведки независимо назвали список узким: `other` собрал 5 фото из 15 |
| D-020 | Explore завершён, переходим к стадии **build** | Критерий D-011 при этом **не проверен** реальным прогоном: треки A и C — кабинетная работа, B пройден глазами. Решение не снимает риск, а переносит его в build |
| D-022 | Тайбрейка нет: решение без согласия обоих не принимается — работа по нему стоит, пока стороны не сойдутся | Алексей снял собственное предложение «делать оба варианта и мерить» |
| D-023 | Механический гейт на `master` — **пока не нужен**, держится на дисциплине D-008 | Алексей сошёлся с позицией Григория |
| D-024 | Для категории `electronics` сервис **не советует вскрытие корпуса вовсе** — не дисклеймер, а полный отказ от таких сценариев | Усилено D-012: адресат — незнакомый человек, не автор. Более широкий вопрос (общий механизм безопасности советов) вынесен в `OPEN_ISSUES.md` как D-028 |
| D-025 | Провайдер **не меняется** из-за способа оплаты: смена провайдера и поиск рублёвой оплаты не требуются | Счёт задан отдельно в D-027 |
| D-026 | Состав общего фотонабора — **100+ фотографий, включая взятые из интернета**, не только исходные 15 | Ломает пороги D-011 («≥10 из 15», привязка к `item_10`/`item_13`) — критерий придётся пересмотреть. Вместе с D-018 означает публикацию чужих изображений: вопрос лицензий не решён |
| D-027 | Оплата OpenRouter — **с аккаунта `tonclap`** | Алексей согласился со счётом Григория вместо своего |

Номер **D-006 пропущен намеренно**: его занимала отозванная строка (коммиты `0ad39f7` →
`909d695`). Переиспользовать номер — путать историю. D-002 отменён, D-021 и D-028 не решены.

## Отменённые и пересмотренные

Решение перестало действовать — не удаляем, а переносим сюда с датой и причиной.
История передумываний — часть эксперимента.

| # | Решение | Что стало | Когда и почему |
| --- | --- | --- | --- |
| ~~D-002~~ | ~~Канал работы — общий репозиторий с прямым доступом, **без форка и PR**~~ | **Отменено** | 01.09.2026. Вытеснено практикой: D-008 сделал PR обязательным транспортом для решений, D-003 — для кода и конфигурации. Прямой доступ к репозиторию сохранился (форка по-прежнему нет), но «без PR» перестало быть правдой в тот же день |
| D-003 (ред. 2) | Прямым push — документы процесса, **включая `COMMON_DECISION.md`**; через PR — код и конфигурация | Исправлено, см. D-003 | 01.09.2026. Список прямого push противоречил D-008, который закрывает прямой push именно в этот файл. Противоречие внутри реестра, а не смена намерения |
| D-003 (ред. 1) | Прямой push в `master` без ревью — допустим только в корне, в подпапках через PR | Переформулировано, см. D-003 | 01.09.2026. Граница по расположению файла давала обратный результат: код в корне шёл бы без ревью, а документы в подпапках — через PR |
| D-009 (ред. 1) | Форма продукта — сравнение сценариев: ранжировать применимые из пяти по паре «время человека / остаточная ценность» | Отклонено до подписей, см. D-009 | 01.09.2026. Григорий снял своё предложение и поддержал сужение до reuse-only: в узкой нише конкурент найден точнее, а расширение уводило продукт в поле DeclutterNow/ResaleScan |
| D-010 (ред. 1) | Прогон на три плеча: `claude-opus-5`, второй облачный провайдер, локальная Gemma 4 12B через Ollama | Отклонено до подписей, см. D-010 | 01.09.2026. Выбран OpenRouter как единый транспорт. Цена — приватность: локальное плечо давало «фото не покидает устройство» |
